import { Car } from "./filterTypes";

export type FormPrimaryProps = {
  carInfo: Car;
};

export type CarInfoProps = {
  carInfo: Car;
};

export type FooterIcon = {
  icon: string;
  link: string;
};

export type OrderProps = {
  cars: Car[];
};
